# Optional: base URL of your deployed app (for remote sync). Leave empty to use current origin.
# Example: https://your-project.vercel.app
VITE_API_URL=https://tars-xi.vercel.app

# Required only for Vercel API (api/state.js). Set in Vercel project Environment Variables.
# UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=xxx
